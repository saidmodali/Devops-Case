apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: devops-case
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: host.docker.internal:5000/devops-frontend:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80